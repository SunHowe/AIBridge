interface:
  display_name: "AIBridge Development Workflow"
  short_description: "多分支 Unity 开发、调试、审查与验证工作流指南"
  default_prompt: "Use $aibridge-development-workflow to route a Unity/AIBridge task into implementation, debug investigation, review, validation, or workflow orchestration."

policy:
  allow_implicit_invocation: true
